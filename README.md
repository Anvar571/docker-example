# Docker-example

Project run with docker
```
docker compose up -d or docker-compose ud -d
```

Project run without docker

Developer evn

```
npm run start:dev
```

Production env

```
npm run start:prod
```